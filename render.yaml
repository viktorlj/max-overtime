services:
  - type: web
    name: max-overtime
    runtime: python
    buildCommand: pip install -e .
    startCommand: gunicorn run:server --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.11"
